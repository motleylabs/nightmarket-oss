#import "./profile.graphql"
#import "./listingpreview.graphql"
#import "./listing.graphql"

fragment NftInfo on Nft {
  image
  description
  mintAddress
  address
  name
  shortMintAddress @client
  moonrankRank
  owner {
    address
  }
  creators {
    address
    displayName @client
    previewImage @client
    profile {
      ...ProfileInfo
    }
    metadataAddress
    share
    verified
    position
  }
  moonrankCollection {
    name
    id
    image(width: 100)
  }
  purchases {
    id
    solPrice @client
    price
    buyer
    seller
  }
  listings {
    ...ListingInfo
  }
}

fragment NftListingInfo on Nft {
  ...NftInfo

  listing @client {
    ...ListingInfo
  }
  listings {
    ...ListingInfo
  }
}
