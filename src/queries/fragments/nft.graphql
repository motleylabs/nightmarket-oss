#import "./profile.graphql"
#import "./listingpreview.graphql"
#import "./listing.graphql"

fragment NftInfo on Nft {
  image
  description
  mintAddress
  address
  name
  shortMintAddress @client
  owner {
    address
  }
  creators {
    address
    displayName @client
    previewImage @client
    profile {
      ...ProfileInfo
    }
    metadataAddress
    share
    verified
    position
  }
  moonrankCollection {
    name
    id
    image(width: 100)
  }
  purchases {
    id
    previewPrice @client
    price
    buyer
    seller
  }
  listings {
    ...ListingPreview
    previewPrice @client
  }
  magicEdenListings @client {
    ...ListingPreview
    previewPrice @client
  }
  marketplaceListings @client {
    ...ListingPreview
    previewPrice @client
  }
}

fragment NftListingInfo on Nft {
  ...NftInfo

  listing @client {
    ...ListingInfo
  }
  listings {
    ...ListingInfo
  }
}
