query ReportQuery($address: String!, $startDate: DateTimeUtc, $endDate: DateTimeUtc)
@cached(ttl: 300) {
  auctionHouse(address: $address) {
    address
    volume(startDate: $startDate, endDate: $endDate)
  }
  solanaNetwork {
    tps
    price(currency: USD)
  }
}
