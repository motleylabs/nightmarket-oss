overwrite: true
documents:
  - '**/*.graphql'
schema:
  - https://graph.holaplex.com/v1
  - local.graphql
generates:
  ./@types/graphql.d.ts:
    plugins:
      - typescript-graphql-files-modules
  ./src/graphql.types.ts:
    plugins:
      - typescript
